<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="LabTest.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:LabTest.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="LabTest">

    <TabBar Route="main">
        <Tab Title="Question1" Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate view:QuestionOne}">
            </ShellContent>
        </Tab>
        <Tab Title="Question2" Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate view:QuestionTwo}">
            </ShellContent>
        </Tab>
        <Tab Title="Question3" Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate view:QuestionThree}">
            </ShellContent>
        </Tab>
    </TabBar>

</Shell>
